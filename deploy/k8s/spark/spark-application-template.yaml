apiVersion: v1
kind: ConfigMap
metadata:
  name: spark-application-template
  namespace: ais
  labels:
    app.kubernetes.io/name: spark-application-template
    app.kubernetes.io/part-of: atmospheric-intelligence-system
data:
  README: |
    Placeholder for a future SparkApplication CRD if Spark Operator is installed.
    TODO3 uses spark-submit --master k8s:// as the primary runtime.
