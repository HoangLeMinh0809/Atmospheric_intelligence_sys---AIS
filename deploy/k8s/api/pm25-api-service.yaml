apiVersion: v1
kind: Service
metadata:
  name: pm25-api
  namespace: ais
  labels:
    app.kubernetes.io/name: pm25-api
    app.kubernetes.io/part-of: atmospheric-intelligence-system
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pm25-api
  ports:
    - name: http
      port: 80
      targetPort: http
