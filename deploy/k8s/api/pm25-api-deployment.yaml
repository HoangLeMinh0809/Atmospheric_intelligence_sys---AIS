apiVersion: apps/v1
kind: Deployment
metadata:
  name: pm25-api
  namespace: ais
  labels:
    app.kubernetes.io/name: pm25-api
    app.kubernetes.io/part-of: atmospheric-intelligence-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: pm25-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: pm25-api
        app.kubernetes.io/part-of: atmospheric-intelligence-system
    spec:
      serviceAccountName: ais-workload
      containers:
        - name: api
          image: ais-pm25-api:local
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: ais-runtime-config
            - secretRef:
                name: ais-runtime-secrets
                optional: true
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
            timeoutSeconds: 2
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
